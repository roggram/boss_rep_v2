<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新規コミュニティ追加</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/add_trigger.css">
</head>
<body>
    <h1>新規コミュニティ名追加</h1>

    <div class="add_form_container">
        <form method="post" action="/add_trigger_exec">
            <div class="input_group">
                <input
                    type="text"
                    name="trigger_name"
                    placeholder="例:xxxサークル"
                    value="{% if old_input.trigger_name is defined %}{{ old_input.trigger_name }}{% endif %}"
                    class="{% if validation_errors.trigger_name is defined %}error{% endif %}"
                    required
                    autocomplete="off"
                >
                <button type="submit" class="add_button">
                    <i class="fas fa-plus"></i>
                    追加
                </button>
            </div>
            {% if validation_errors.trigger_name is defined %}
                <span class="error-message">{{ validation_errors.trigger_name|first }}</span>
            {% endif %}
        </form>

        <div class="button_container">
            <form method="get" action="/show_trigger">
                <button type="submit" class="back_button">
                    <i class="fas fa-arrow-left"></i>
                    戻る
                </button>
            </form>
        </div>
    </div>
</body>
</html>
