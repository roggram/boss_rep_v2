<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>メンバーの名前の編集</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/edit_situation.css">
</head>
<body>
    <h1>メンバーの名前の編集</h1>

    <div class="nav-buttons">
        <button type="button" onclick="history.back()" class="back-button">
            <i class="fas fa-arrow-left"></i>
            戻る
        </button>
        <a href="show_trigger" class="home-button">
            <i class="fas fa-home"></i>
            ホームに戻る
        </a>
    </div>

    {% for situation in situations %}
        <div class="one_situation_container">
            <form method="post" action="edit_situation_name_exec" class="situation_show_form">
                <input type="hidden" name="trigger_id" value="{{trigger_id}}">
                <input type="hidden" name="situation_id" value="{{situation.id}}">
                <input class="situation_show_button" type="text" name="update_situation_name_text" value="{{ situation.situation_name }}" required>
                <button type="submit" class="message_edit_button">更新</button>
            </form>
        </div>
    {% endfor %}
</body>
</html>
